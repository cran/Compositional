#useDynLib(Compositional, .registration = TRUE)

importFrom("MASS", cov.rob, cov.trob)
importFrom("graphics", filled.contour, contour, lines, plot,
           points, abline, hist, legend, text, image)

importFrom("stats", cor, lm, cov, optim, optimize, quantile, rnorm, sd,
           qnorm, nlminb, nlm, lm.fit, rchisq, deviance, pchisq, qchisq,
           rgamma, mahalanobis, runif, qt, qf, pf, model.matrix, prcomp)

importFrom("grDevices", terrain.colors, grey)
importFrom("Rfast", colVars, colMedians, colmeans, colsums, rowmeans, rowsums,
           diri.nr2, sort_mat, mahala, rmvnorm, rmvt, sort_unique, rowMins,
           rowMaxs, Dist, spat.med, lmfit, dista, Log, Var, knn, knn.cv, cova,
           glm_logistic, glm_poisson, colhameans, spdinv, colnth, Table)

import("doParallel")
import("parallel")
import("foreach")
importFrom("sn", msn.mle, rmsn)
importFrom("mixture", gpcm)
importFrom("fields", image.plot)
importFrom("emplik", el.test)
exportPattern("^[[:alpha:]]+")
exportPattern("^[^\\.]")


